//
//  ServiceProvider.swift
//  Burgan
//
//  Created by Malti Maurya on 17/03/20.
//  Copyright © 2020 1st iMac. All rights reserved.
//

import UIKit




/*
 
65911938
 
// MARK: - Welcome
struct Welcome: Codable {
    let products: [Product]
}

// MARK: - Product
struct Product: Codable {
    let entityID: String
    let typeID: String?
    let sku, createdAt, updatedAt: String
    let sourceVisibility: [String]?
    let weight, absoluteWeight, jewelryMetalType: String
    let purity: String?
    let isApp: String
    let jewelryType: String?
    let productDescription, shortDescription: String
    let metaKeyword, metaDescription, metaTitle: String?
    let name: String
    let frontHeight, totalWeight, topWidth, karat: String?
    let regularPriceWithTax, regularPriceWithoutTax, finalPriceWithTax, finalPriceWithoutTax: Int
    let isSaleable: Bool
    let imageURL: String
    let jewelryMetal: String?

    enum CodingKeys: String, CodingKey {
        case entityID = "entity_id"
        case typeID = "type_id"
        case sku
        case createdAt = "created_at"
        case updatedAt = "updated_at"
        case sourceVisibility = "source_visibility"
        case weight
        case absoluteWeight = "absolute_weight"
        case jewelryMetalType = "jewelry_metal_type"
        case purity
        case isApp = "is_app"
        case jewelryType = "jewelry_type"
        case productDescription = "description"
        case shortDescription = "short_description"
        case metaKeyword = "meta_keyword"
        case metaDescription = "meta_description"
        case metaTitle = "meta_title"
        case name
        case frontHeight = "front_height"
        case totalWeight = "total_weight"
        case topWidth = "top_width"
        case karat
        case regularPriceWithTax = "regular_price_with_tax"
        case regularPriceWithoutTax = "regular_price_without_tax"
        case finalPriceWithTax = "final_price_with_tax"
        case finalPriceWithoutTax = "final_price_without_tax"
        case isSaleable = "is_saleable"
        case imageURL = "image_url"
        case jewelryMetal = "jewelry_metal"
    }
}
*/


